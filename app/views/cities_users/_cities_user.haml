%table.table
  %thead
    %th Ciudad
    %th Usuario
  %tbody
    %tr
      %td
        Hola
    -# - CitiesUser.where.not(user_id: current_user.id).each do |cu|
    -#   %tr{id: "tr-#{cu.id}"}
    -#     %td=cu.city.name
    -#     %td=cu.user.name